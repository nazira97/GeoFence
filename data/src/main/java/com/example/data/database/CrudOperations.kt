package com.example.data.database

/**
 * Created by Parvez on 15/11/18.
 */

open class CrudOperations  {
    /**
     * We've made the required changes as per your review comments(we've made the Realm operation class as Singleton class)
     * and also we've deleted few files(model-cat,dog,person....domain-repository-CRUDRepository....domain-usecase-CRUDUseCase)
     * which was a part of base code, but we were not using now, so please kindly check if those files gets deleted from
     * master branch if not please delete them and also delete this crud operation class.
     */
}